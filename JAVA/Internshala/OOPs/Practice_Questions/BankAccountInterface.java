public interface BankAccountInterface {
    
    //method signatures only, no body of methods
    void deposit(double amount);
    void withdraw(double amount);
    double getBalance();
    // <- these are public and abstract methods 
    
}
